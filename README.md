# token-approval-checker

Check and manage token approvals for security. Detect risky approvals, unlimited allowances, and suspicious spender contracts on EVM chains.

## Installation

```bash
pip install -e .
```

## Usage

```bash
token approval checker --help
```
